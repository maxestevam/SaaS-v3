const defaults = { primaryColor: "#111111", secondaryColor: "#f5f5f5", accentColor: "#e6e6e6", backgroundColor: "#ffffff", textColor: "#111111", fontFamily: "Inter, sans-serif" };
export function selectPublicStorePresentation(domain) { const store = domain?.store || {}; const theme = { ...defaults, ...(store.theme || {}) }; return { storeId: store.id || "", slug: store.slug || "", locale: store.locale || "pt-BR", currency: store.currency || "BRL", theme, capabilities: domain?.capabilities || {} }; }
export function publicThemeStyle(theme = {}) { return { "--store-primary": theme.primaryColor || defaults.primaryColor, "--store-secondary": theme.secondaryColor || defaults.secondaryColor, "--store-accent": theme.accentColor || defaults.accentColor, "--store-background": theme.backgroundColor || defaults.backgroundColor, "--store-text": theme.textColor || defaults.textColor, "--store-font": theme.fontFamily || defaults.fontFamily }; }
