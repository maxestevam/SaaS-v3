import { ArrowLeft, CheckCircle2, CreditCard, Mail, ShieldCheck, Truck } from "lucide-react";
import { useLocation } from "wouter";
import { AppShell } from "@/components/AppShell";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

const callbackUrl = "https://lojadescompl-8wmvnkfz.manus.space/v1/integrations/mercado-pago/callback";

export default function IntegrationGuidePage() {
  const [, navigate] = useLocation();
  return <AppShell><main data-private className="mx-auto w-full max-w-4xl px-5 py-8 sm:px-8 lg:px-12"><Button variant="outline" size="sm" onClick={() => navigate("/settings")}><ArrowLeft className="size-4" />Voltar a configurações</Button><header className="mt-6 border-b pb-7"><p className="text-[10px] font-bold uppercase tracking-[.16em] text-muted-foreground">Guia operacional</p><h1 className="mt-2 text-3xl font-bold tracking-tight">Conecte as integrações da sua loja</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-muted-foreground">Cada conta, token, remetente e modelo é salvo no escopo da loja selecionada. Os valores confidenciais permanecem protegidos no servidor.</p></header><div className="mt-8 space-y-6"><GuideSection icon={CreditCard} title="1. Mercado Pago: receber pagamentos"><p>Antes da primeira conexão, cadastre a URL abaixo como retorno OAuth na aplicação do Mercado Pago. Ela precisa corresponder exatamente ao endereço configurado.</p><code className="mt-4 block break-all rounded-lg border bg-muted p-3 text-xs">{callbackUrl}</code><ol className="mt-4 list-decimal space-y-2 pl-5 text-sm leading-6 text-muted-foreground"><li>Abra <strong className="text-foreground">Configurações → Pagamentos</strong> com a loja correta selecionada.</li><li>Clique em <strong className="text-foreground">Conectar conta do Mercado Pago</strong>.</li><li>Entre no Mercado Pago e aceite o consentimento.</li><li>Ao retornar, confira o estado <strong className="text-foreground">Conectado</strong>. Essa conta será exclusiva da loja escolhida.</li></ol><Notice text="A assinatura do Loja Descomplicada continua usando o fluxo financeiro da plataforma. Esta conexão é destinada aos recebimentos futuros das vendas da loja." /></GuideSection><GuideSection icon={Truck} title="2. Melhor Envio: cotar frete"><p>Gere o token da API no Melhor Envio, informe o CEP de origem da loja e escolha Produção ou Sandbox. Salve e teste o token antes de usar a simulação.</p><ul className="mt-4 space-y-2 text-sm leading-6 text-muted-foreground"><li>As dimensões devem estar em centímetros e o peso em quilogramas.</li><li>Informe valor e quantidade do produto para formar a cotação.</li><li>A integração consulta preços e prazos, mas não compra etiquetas nem agenda postagem.</li></ul></GuideSection><GuideSection icon={Mail} title="3. Resend ou SMTP: enviar e-mails"><p>Escolha o provedor que a loja utilizará. No Resend, salve a chave de API e mantenha um domínio de remetente verificado. Para SMTP, informe host, porta, usuário, senha e o modo de segurança solicitado pelo servidor.</p><div className="mt-4 grid gap-3 sm:grid-cols-2"><div className="rounded-lg border p-4 text-sm"><strong>Resend via SMTP</strong><p className="mt-2 text-muted-foreground">Host: <code>smtp.resend.com</code><br />Usuário: <code>resend</code><br />Senha: chave da API.</p></div><div className="rounded-lg border p-4 text-sm"><strong>Modelos disponíveis</strong><p className="mt-2 text-muted-foreground">Compra confirmada, pedido finalizado e solicitação de pagamento. Edite HTML, assunto e variáveis na aba E-mails.</p></div></div><Notice text="Antes de ativar mensagens reais, salve o remetente e envie um e-mail de teste para conferir domínio, caixa de entrada e links." /></GuideSection></div></main></AppShell>;
}

function GuideSection({ icon: Icon, title, children }) { return <Card><CardHeader className="border-b"><CardTitle className="flex items-center gap-3"><span className="flex size-10 items-center justify-center rounded-xl bg-muted"><Icon className="size-5" /></span>{title}</CardTitle></CardHeader><CardContent className="space-y-3 p-6 text-sm leading-6 text-muted-foreground">{children}</CardContent></Card>; }
function Notice({ text }) { return <div className="mt-5 flex gap-3 rounded-xl border border-emerald-500/20 bg-emerald-500/5 p-4 text-sm text-emerald-900 dark:text-emerald-200"><ShieldCheck className="mt-0.5 size-5 shrink-0 text-emerald-600" /><p>{text}</p></div>; }
