import { boundedPagination, RequestError } from "../shared/http.js";
export const validStates = ["AC", "AL", "AP", "AM", "BA", "CE", "DF", "ES", "GO", "MA", "MT", "MS", "MG", "PA", "PB", "PR", "PE", "PI", "RJ", "RN", "RS", "RO", "RR", "SC", "SP", "SE", "TO"];
const types = ["percentage", "fixed", "free_shipping"];
export function parseCouponFilters(source) { const { page, limit, offset } = boundedPagination(source, { defaultLimit: 20, maxLimit: 100, maxPage: 100000 }); const status = source?.status === undefined ? "" : String(source.status); const query = source?.q === undefined ? "" : String(source.q).trim().toUpperCase(); if (status && !["active", "inactive", "expired"].includes(status)) throw new RequestError(422, "Informe um status de cupom válido."); if (query.length > 64) throw new RequestError(422, "Informe uma busca de cupons com até 64 caracteres."); return { page, limit, offset, query, status }; }
export function parseCouponInput(source = {}, existing = null) { const read = (key, fallback = "") => source[key] === undefined ? fallback : source[key]; const code = normalizeCode(read("code", existing?.code)); const discountType = String(read("discountType", existing?.discount_type)).trim(); const percentageOff = discountType === "percentage" ? parseAmount(read("percentageOff", existing?.percentage_off), 0.01, 100) : null; const amountOffCents = discountType === "fixed" ? parseInteger(read("amountOffCents", existing?.amount_off_cents), 1, 10_000_000) : null; const minimumOrderCents = parseInteger(read("minimumOrderCents", existing?.minimum_order_cents), 0, 100_000_000); const expiresAt = nullableTimestamp(read("expiresAt", existing?.expires_at)); const usageLimit = nullableInteger(read("usageLimit", existing?.usage_limit), 1, 10_000_000); const freeShippingStates = parseStates(read("freeShippingStates", existing?.free_shipping_states)); const active = parseBoolean(read("active", existing?.active ?? true)); const newUsersOnly = parseBoolean(read("newUsersOnly", existing?.new_users_only ?? false)); if (!code) throw new RequestError(422, "Informe um código de cupom com letras, números, hífen ou sublinhado."); if (!types.includes(discountType)) throw new RequestError(422, "Selecione um tipo de benefício válido."); if (discountType === "percentage" && percentageOff === null) throw new RequestError(422, "Informe uma porcentagem entre 0,01% e 100%."); if (discountType === "fixed" && amountOffCents === null) throw new RequestError(422, "Informe um valor fixo de desconto válido."); if (minimumOrderCents === null) throw new RequestError(422, "Informe um valor mínimo de compra válido."); if (expiresAt === false) throw new RequestError(422, "Informe uma data de validade válida."); if (usageLimit === false) throw new RequestError(422, "Informe uma quantidade máxima de usos válida."); if (freeShippingStates === false) throw new RequestError(422, "Informe estados válidos para o frete grátis."); if (active === null) throw new RequestError(422, "Informe se o cupom está ativo."); if (newUsersOnly === null) throw new RequestError(422, "Informe se o cupom é exclusivo para novos usuários."); return { code, discountType, percentageOff, amountOffCents, minimumOrderCents, expiresAt, usageLimit, freeShippingStates: discountType === "free_shipping" ? freeShippingStates : [], active, newUsersOnly }; }
export function publicCoupon(row) { return { id: row.id, storeId: row.store_id || row.storeId, code: row.code, discountType: row.discount_type || row.discountType, percentageOff: row.percentage_off == null ? null : Number(row.percentage_off), amountOffCents: row.amount_off_cents == null ? null : Number(row.amount_off_cents), minimumOrderCents: Number(row.minimum_order_cents || row.minimumOrderCents || 0), expiresAt: row.expires_at == null ? null : Number(row.expires_at), usageLimit: row.usage_limit == null ? null : Number(row.usage_limit), usageCount: Number(row.usage_count || row.usageCount || 0), freeShippingStates: uniqueStates(row.free_shipping_states || row.freeShippingStates), active: Boolean(row.active), newUsersOnly: Boolean(row.new_users_only ?? row.newUsersOnly), createdAt: Number(row.created_at || row.createdAt || 0), updatedAt: Number(row.updated_at || row.updatedAt || 0) }; }
function normalizeCode(value) { return String(value || "").trim().toUpperCase().replace(/\s+/g, "-").replace(/[^A-Z0-9_-]/g, "").slice(0, 64); } function parseInteger(value, min, max) { const number = Number(value); return Number.isInteger(number) && number >= min && number <= max ? number : null; } function nullableInteger(value, min, max) { if (value == null || value === "") return null; const number = parseInteger(value, min, max); return number === null ? false : number; } function parseAmount(value, min, max) { const number = Number(value); return Number.isFinite(number) && number >= min && number <= max ? Number(number.toFixed(2)) : null; } function nullableTimestamp(value) { if (value == null || value === "") return null; const timestamp = Number(value); return Number.isFinite(timestamp) && timestamp > 0 ? Math.floor(timestamp) : false; } function parseBoolean(value) { if (value === true || value === 1) return true; if (value === false || value === 0) return false; return null; } function parseStates(value) { if (value == null || value === "") return []; let source = value; if (typeof source === "string") { try { source = JSON.parse(source); } catch { return false; } } if (!Array.isArray(source)) return false; const states = source.map((state) => String(state || "").trim().toUpperCase()); return states.every((state) => validStates.includes(state)) ? [...new Set(states)] : false; } function uniqueStates(value) { let source = value; if (typeof source === "string") { try { source = JSON.parse(source); } catch { source = []; } } return [...new Set(Array.isArray(source) ? source.map((state) => String(state || "").trim().toUpperCase()).filter((state) => validStates.includes(state)) : [])]; }
