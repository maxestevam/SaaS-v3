export class AccountValidationError extends Error { constructor(status, message) { super(message); this.status = status; } }
export function parseAccountProfile(source = {}) { const name = String(source.name || "").trim(); const email = String(source.email || "").trim().toLowerCase(); if (name.length < 2 || !/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)) throw new AccountValidationError(422, "Informe nome e e-mail válidos."); return { name, email }; }
export function requiredPassword(value, message) { const password = String(value || ""); if (!password) throw new AccountValidationError(422, message); return password; }
export function parsePasswordChange(source = {}) { const currentPassword = requiredPassword(source.currentPassword, "Informe a senha atual."); const newPassword = requiredPassword(source.newPassword, "Informe a nova senha."); if (newPassword.length < 8) throw new AccountValidationError(422, "A nova senha precisa ter pelo menos 8 caracteres."); return { currentPassword, newPassword }; }
