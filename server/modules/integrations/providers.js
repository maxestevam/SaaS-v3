import nodemailer from "nodemailer";
export async function melhorEnvioRequest(integration, path, { contactEmail, method = "GET", body } = {}) { const base = integration.metadata?.environment === "sandbox" ? "https://sandbox.melhorenvio.com.br" : "https://melhorenvio.com.br"; return fetch(`${base}${path}`, { method, headers: { Accept: "application/json", "Content-Type": "application/json", Authorization: `Bearer ${integration.config.accessToken}`, "User-Agent": `Loja Descomplicada (${contactEmail || "integracao@local"})` }, body: body ? JSON.stringify(body) : undefined, signal: AbortSignal.timeout(15000) }); }
export async function verifyResendApiKey(apiKey) { return fetch("https://api.resend.com/domains", { headers: { Authorization: `Bearer ${apiKey}` }, signal: AbortSignal.timeout(10000) }); }
export async function sendResendEmail({ apiKey, idempotencyKey, from, to, subject, html, replyTo }) { return fetch("https://api.resend.com/emails", { method: "POST", headers: { Authorization: `Bearer ${apiKey}`, "Content-Type": "application/json", "Idempotency-Key": idempotencyKey }, body: JSON.stringify({ from, to: [to], subject, html, reply_to: replyTo || undefined }), signal: AbortSignal.timeout(15000) }); }
export function smtpTransport(config) { return nodemailer.createTransport({ host: config.host, port: Number(config.port), secure: Boolean(config.secure), auth: { user: config.username, pass: config.password }, connectionTimeout: 10000, greetingTimeout: 10000, socketTimeout: 15000 }); }
