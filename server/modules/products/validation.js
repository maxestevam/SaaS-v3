import { boundedPagination } from "../shared/http.js";

const PAGE_SIZES = [10, 20, 50, 100];
export const productMediaFormats = { "image/jpeg": { kind: "image", extension: "jpg" }, "image/png": { kind: "image", extension: "png" }, "image/webp": { kind: "image", extension: "webp" }, "video/mp4": { kind: "video", extension: "mp4" }, "video/webm": { kind: "video", extension: "webm" } };

export class ProductValidationError extends Error { constructor(status, message) { super(message); this.status = status; } }

export function parseCategoryInput(source = {}) { const name = validText(source.name, 2, 120); const description = validText(source.description || "", 0, 500); const active = source.active === undefined ? true : parseBoolean(source.active); const suppliedParent = source.parentCategoryId === undefined ? source.parent_category_id : source.parentCategoryId; const parentCategoryId = suppliedParent === undefined || suppliedParent === null || suppliedParent === "" ? null : validUuid(suppliedParent); const cropAspect = ["1:1", "9:16", "16:9"].includes(String(source.cropAspect || source.crop_aspect || "1:1")) ? String(source.cropAspect || source.crop_aspect || "1:1") : ""; const cardImageUrl = optionalHttpUrl(source.cardImageUrl ?? source.card_image_url); const heroImageUrl = optionalHttpUrl(source.heroImageUrl ?? source.hero_image_url); if (!name) throw new ProductValidationError(422, "Informe um nome de categoria com ao menos 2 caracteres."); if (active === null) throw new ProductValidationError(422, "Informe se a categoria está ativa."); if (!cropAspect) throw new ProductValidationError(422, "Selecione um padrão de recorte válido."); if (cardImageUrl === false || heroImageUrl === false) throw new ProductValidationError(422, "Informe URLs HTTPS válidas para as imagens da categoria."); if (suppliedParent !== undefined && suppliedParent !== null && suppliedParent !== "" && !parentCategoryId) throw new ProductValidationError(422, "Selecione uma categoria principal válida."); return { name, description, active, parentCategoryId, cropAspect, cardImageUrl, heroImageUrl }; }
export function parseProductInput(source, { allowEmptyUploads = false } = {}) { const name = validText(source?.name, 2, 160); const sku = optionalSku(source?.sku); const description = validText(source?.description || "", 0, 10000); const shortDescription = validText(source?.shortDescription ?? source?.short_description ?? "", 0, 500); const categoryId = validUuid(source?.categoryId === undefined ? source?.category_id : source.categoryId); const priceCents = Number(source?.priceCents === undefined ? source?.price_cents : source.priceCents); const compareAtPriceCents = optionalCents(source?.compareAtPriceCents ?? source?.compare_at_price_cents); const costPriceCents = optionalCents(source?.costPriceCents ?? source?.cost_price_cents); const brand = validText(source?.brand || "", 0, 120); const tags = parseTags(source?.tags ?? source?.tags_json); const stockQuantity = nonNegativeInteger(source?.stockQuantity ?? source?.stock_quantity ?? 0); const weightGrams = optionalMeasurement(source?.weightGrams ?? source?.weight_grams); const widthMm = optionalMeasurement(source?.widthMm ?? source?.width_mm); const heightMm = optionalMeasurement(source?.heightMm ?? source?.height_mm); const depthMm = optionalMeasurement(source?.depthMm ?? source?.depth_mm); const variants = parseVariants(source?.variants); const suppliedStatus = source?.status; const status = suppliedStatus === undefined ? "active" : (["active", "draft", "archived"].includes(suppliedStatus) ? suppliedStatus : ""); const draftId = validUuid(source?.draftId); const rawUploadIds = source?.uploadIds; const uploadIds = Array.isArray(rawUploadIds) ? [...new Set(rawUploadIds.map(validUuid).filter(Boolean))] : rawUploadIds === undefined ? [] : null; if (!name) return invalid("Informe um nome de produto válido."); if (source?.sku && sku === null) return invalid("Informe um SKU válido."); if (!categoryId) return invalid("Selecione uma categoria válida."); if (!status) return invalid("Selecione um status de produto válido."); if (!Number.isInteger(priceCents) || priceCents < 0 || priceCents > 2147483647) return invalid("Informe um preço válido."); if (compareAtPriceCents === false || costPriceCents === false) return invalid("Informe os preços complementares em centavos válidos."); if (!brand && source?.brand) return invalid("Informe uma marca válida."); if (!Array.isArray(tags)) return invalid("Informe até 20 tags válidas."); if (stockQuantity === null) return invalid("Informe um estoque válido."); if ([weightGrams, widthMm, heightMm, depthMm].includes(false)) return invalid("Informe peso e dimensões válidos."); if (!Array.isArray(variants)) return invalid("Revise as variantes do produto."); if (!Array.isArray(uploadIds)) return invalid("Informe uma lista válida de mídias."); if (!allowEmptyUploads && (!draftId || !uploadIds.length)) return invalid("Adicione ao menos uma mídia antes de salvar."); if (uploadIds.length > 5) return invalid("Quantidade de mídias inválida."); return { valid: true, value: { name, sku, description, shortDescription, categoryId, priceCents, compareAtPriceCents, costPriceCents, brand, tags, stockQuantity, weightGrams, widthMm, heightMm, depthMm, variants, status, draftId, uploadIds } }; }
export function getProductListInput(source = {}) { if (source.limit !== undefined && !PAGE_SIZES.includes(Number(source.limit))) throw new ProductValidationError(422, "Use um limite de 10, 20, 50 ou 100 produtos por página."); if (source.sort && !["recent", "oldest", "name", "price_low", "price_high"].includes(String(source.sort))) throw new ProductValidationError(422, "Informe uma ordenação de produtos válida."); const pagination = boundedPagination(source, { defaultLimit: 20, maxLimit: 100, maxPage: 100000 }); if (!PAGE_SIZES.includes(pagination.limit)) throw new ProductValidationError(422, "Use um limite de 10, 20, 50 ou 100 produtos por página."); return { pagination, filters: buildProductFilters(String(source.storeId || ""), source), sort: sortClause(source.sort) }; }
export function parsePagination(source) { const rawPage = Number(source?.page || 1); const rawLimit = Number(source?.limit || 20); const page = Number.isInteger(rawPage) && rawPage >= 1 && rawPage <= 100000 ? rawPage : 1; const limit = PAGE_SIZES.includes(rawLimit) ? rawLimit : 20; return { page, limit, offset: (page - 1) * limit }; }
export function buildProductFilters(storeId, source = {}) { const params = [storeId]; let where = "products.store_id = ?"; const search = validText(source.search || "", 0, 120); const categoryId = validUuid(source.categoryId); const dateFrom = validTimestamp(source.dateFrom); const dateTo = validTimestamp(source.dateTo); if (source.categoryId !== undefined && !categoryId) throw new ProductValidationError(422, "Informe uma categoria válida para filtrar produtos."); if (source.dateFrom !== undefined && !dateFrom) throw new ProductValidationError(422, "Informe uma data inicial válida para filtrar produtos."); if (source.dateTo !== undefined && !dateTo) throw new ProductValidationError(422, "Informe uma data final válida para filtrar produtos."); if (dateFrom && dateTo && dateFrom > dateTo) throw new ProductValidationError(422, "A data inicial não pode ser maior que a data final."); if (search) { where += " AND (products.name LIKE ? OR products.description LIKE ?)"; params.push(`%${search}%`, `%${search}%`); } if (categoryId) { where += " AND products.category_id = ?"; params.push(categoryId); } if (dateFrom) { where += " AND products.created_at >= ?"; params.push(dateFrom); } if (dateTo) { where += " AND products.created_at <= ?"; params.push(dateTo); } return { where, params }; }
export function assertValidProductInput(parsed) { if (!parsed.valid) throw new ProductValidationError(422, parsed.error); return parsed.value; }
export function getProductUploadInput(body, file) { const draftId = validUuid(body?.draftId); const format = productMediaFormats[file?.mimetype]; if (!draftId || !file) throw new ProductValidationError(422, "Envie uma mídia e um identificador temporário válido."); if (!format || !matchesMediaSignature(file.buffer, file.mimetype)) throw new ProductValidationError(422, "O tipo de arquivo enviado não é permitido."); const maximum = format.kind === "image" ? 8 * 1024 * 1024 : 45 * 1024 * 1024; if (file.size > maximum) throw new ProductValidationError(413, "O arquivo excede o tamanho máximo permitido."); return { draftId, format }; }
export function matchesMediaSignature(buffer, mime) { if (!Buffer.isBuffer(buffer) || buffer.length < 12) return false; if (mime === "image/jpeg") return buffer[0] === 0xff && buffer[1] === 0xd8 && buffer[2] === 0xff; if (mime === "image/png") return buffer.subarray(0, 8).equals(Buffer.from([0x89, 0x50, 0x4e, 0x47, 0x0d, 0x0a, 0x1a, 0x0a])); if (mime === "image/webp") return buffer.subarray(0, 4).toString() === "RIFF" && buffer.subarray(8, 12).toString() === "WEBP"; if (mime === "video/webm") return buffer.subarray(0, 4).equals(Buffer.from([0x1a, 0x45, 0xdf, 0xa3])); if (mime === "video/mp4") return buffer.subarray(4, 8).toString() === "ftyp"; return false; }
function invalid(error) { return { valid: false, error }; }
function sortClause(sort) { return ({ recent: "products.created_at DESC", oldest: "products.created_at ASC", name: "products.name ASC", price_low: "products.price_cents ASC", price_high: "products.price_cents DESC" })[String(sort || "")] || "products.created_at DESC"; }
function parseBoolean(value) { if (value === true || value === 1) return true; if (value === false || value === 0) return false; return null; }
function validText(value, min, max) { const text = String(value || "").trim(); return text.length >= min && text.length <= max ? text : ""; }
function validUuid(value) { const id = String(value || "").trim(); return /^[a-f0-9]{8}-[a-f0-9]{4}-[1-5][a-f0-9]{3}-[89ab][a-f0-9]{3}-[a-f0-9]{12}$/i.test(id) ? id : ""; }
function validTimestamp(value) { const timestamp = Number(value); return Number.isSafeInteger(timestamp) && timestamp > 0 ? timestamp : 0; }
function optionalSku(value) { if (value === undefined || value === null || String(value).trim() === "") return ""; const sku = String(value).trim(); return /^[a-z0-9][a-z0-9._/-]{0,119}$/i.test(sku) ? sku : null; }
function optionalCents(value) { if (value === undefined || value === null || value === "") return null; const cents = Number(value); return Number.isInteger(cents) && cents >= 0 && cents <= 2147483647 ? cents : false; }
function nonNegativeInteger(value) { const quantity = Number(value); return Number.isInteger(quantity) && quantity >= 0 && quantity <= 2147483647 ? quantity : null; }
function optionalMeasurement(value) { if (value === undefined || value === null || value === "") return null; const measurement = Number(value); return Number.isInteger(measurement) && measurement >= 0 && measurement <= 2147483647 ? measurement : false; }
function optionalHttpUrl(value) { if (value === undefined || value === null || value === "") return null; const url = String(value).trim(); return /^https:\/\/.{1,780}$/i.test(url) ? url : false; }
function parseTags(value) { const raw = value === undefined || value === null || value === "" ? [] : (typeof value === "string" ? safeJson(value, []) : value); if (!Array.isArray(raw) || raw.length > 20) return null; const tags = [...new Set(raw.map((tag) => validText(tag, 1, 60)).filter(Boolean))]; return tags.length === raw.filter((tag) => String(tag || "").trim()).length ? tags : null; }
function parseVariants(value) { const raw = value === undefined || value === null || value === "" ? [] : value; if (!Array.isArray(raw) || raw.length > 100) return null; const seenSkus = new Set(); const variants = []; for (const item of raw) { const name = validText(item?.name, 1, 160); const sku = optionalSku(item?.sku); const priceCents = optionalCents(item?.priceCents ?? item?.price_cents); const stockQuantity = nonNegativeInteger(item?.stockQuantity ?? item?.stock_quantity ?? 0); if (!name || sku === null || priceCents === false || stockQuantity === null || (sku && seenSkus.has(sku.toLowerCase()))) return null; if (sku) seenSkus.add(sku.toLowerCase()); variants.push({ name, sku, priceCents, stockQuantity }); } return variants; }
function safeJson(value, fallback) { try { return JSON.parse(value); } catch { return fallback; } }
