import { Router } from "express"; import { RequestError } from "../shared/http.js";
const router = Router(); const states = new Set(["AC","AL","AP","AM","BA","CE","DF","ES","GO","MA","MT","MS","MG","PA","PB","PR","PE","PI","RJ","RN","RS","RO","RR","SC","SP","SE","TO"]);
router.get("/public/address/cep/:cep", async (req, res, next) => { const cep = String(req.params.cep || "").replace(/\D/g, ""); if (cep.length !== 8) return next(new RequestError(422, "Informe os oito dígitos do CEP.", "INVALID_CEP")); try { const response = await fetch(`https://viacep.com.br/ws/${cep}/json/`); if (!response.ok) return res.status(502).json({ error: "Não foi possível consultar este CEP agora.", code: "ADDRESS_PROVIDER_UNAVAILABLE" }); const data = await response.json(); if (data?.erro) return res.status(404).json({ error: "CEP não encontrado.", code: "CEP_NOT_FOUND" }); return res.json({ address: { postalCode: data.cep || cep, street: data.logradouro || "", complement: data.complemento || "", district: data.bairro || "", city: data.localidade || "", state: states.has(data.uf) ? data.uf : "", country: "BR" } }); } catch { return res.status(502).json({ error: "Não foi possível consultar este CEP agora.", code: "ADDRESS_PROVIDER_UNAVAILABLE" }); } });
export default router;
