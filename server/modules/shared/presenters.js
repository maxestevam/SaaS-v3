export function publicUser(user) { return { id: user.id, name: user.name, email: user.email, onboardingComplete: Boolean(user.onboarding_completed_at) }; }
export function publicStore(store) { return { id: store.id, name: store.name, slug: store.slug, addressMode: store.address_mode || store.addressMode || "slug", customDomain: store.custom_domain || null, description: store.description, storeCategory: store.store_category || store.storeCategory || "", color: store.color, logoUrl: store.logo_url || store.logoUrl || null, fontFamily: store.font_family || store.fontFamily || "inter", contact: { email: store.contact_email || "", phone: store.contact_phone || "", whatsapp: store.whatsapp_phone || "", hours: store.support_hours || "" }, address: { postalCode: store.address_postal_code || "", street: store.address_street || "", number: store.address_number || "", complement: store.address_complement || "", observation: store.address_observation || "", district: store.address_district || "", city: store.address_city || "", state: store.address_state || "", country: store.address_country || "BR" }, socials: { instagram: store.instagram_url || "", facebook: store.facebook_url || "", tiktok: store.tiktok_url || "", youtube: store.youtube_url || "", pinterest: store.pinterest_url || "", twitter: store.twitter_url || "" }, about: { title: store.about_title || "", body: store.about_body || "" }, status: Number(store.status ?? 2), createdAt: store.created_at || store.createdAt, subscription: store.subscription_plan_id ? { planId: store.subscription_plan_id, status: store.subscription_status, trialEndsAt: store.subscription_trial_ends_at } : null, catalog: publicCatalogMetrics(store.catalog), storefront: { maintenance: Boolean(store.maintenance), maintenanceMessage: store.maintenance_message || "", currency: store.currency || "BRL", locale: store.locale || "pt-BR", timezone: store.timezone || "America/Sao_Paulo", template: store.template || "default", faviconUrl: store.favicon_url || null, theme: { primaryColor: store.color || "#FF32B2", secondaryColor: store.theme_secondary_color || null, accentColor: store.theme_accent_color || null, backgroundColor: store.theme_background_color || null, textColor: store.theme_text_color || null } }, settings: parseJson(store.settings_json, {}), paymentMethods: parseJson(store.payment_methods_json, []), shippingMethods: parseJson(store.shipping_methods_json, []) }; }
function parseJson(value, fallback) { try { const parsed = typeof value === "string" ? JSON.parse(value) : value; return parsed ?? fallback; } catch { return fallback; } }
function publicCatalogMetrics(catalog = {}) { return { totalProducts: Number(catalog.totalProducts || 0), activeProducts: Number(catalog.activeProducts || 0), draftProducts: Number(catalog.draftProducts || 0), archivedProducts: Number(catalog.archivedProducts || 0), categoriesWithProducts: Number(catalog.categoriesWithProducts || 0), inventoryUnits: Number(catalog.inventoryUnits || 0), outOfStockProducts: Number(catalog.outOfStockProducts || 0), lowStockProducts: Number(catalog.lowStockProducts || 0), newProductsLast30Days: Number(catalog.newProductsLast30Days || 0) }; }
export function publicSubscription(item) { return { id: item.id, storeId: item.store_id, planId: item.plan_id, status: item.status, amountCents: Number(item.amount_cents || 0), trialEndsAt: item.trial_ends_at, currentPeriodEndsAt: item.current_period_ends_at, renewalDueAt: item.renewal_due_at, graceEndsAt: item.grace_ends_at }; }
export function publicOrder(item) { return { id: item.id, storeId: item.store_id, subscriptionId: item.subscription_id, kind: item.kind, status: item.status, amountCents: item.amount_cents, currency: item.currency, dueAt: item.due_at, paidAt: item.paid_at, initPoint: item.checkout_init_point, pixQrCode: item.pix_qr_code, pixQrCodeBase64: item.pix_qr_code_base64, createdAt: item.created_at }; }
export function publicPlan(item) { let features = []; try { features = Array.isArray(item.features_json) ? item.features_json : JSON.parse(item.features_json || "[]"); } catch { features = []; } return { id: item.id, name: item.name, description: item.description, features, amountCents: Number(item.amount_cents), featured: Boolean(item.is_featured) }; }
